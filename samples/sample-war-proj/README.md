This is a very simple Spring Boot based project that produces a WAR artifact. The application once started up has two 
endpoints:

```
curl -k http://<url>/
OR
curl -k http://<url>/ping
```

returns a `pong` message